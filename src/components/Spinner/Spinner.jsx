import React from 'react';

const Spinner = () => {
    return (
        <div className='h-screen flex justify-center'>
            Loading .. ....................
        </div>
    );
};

export default Spinner;